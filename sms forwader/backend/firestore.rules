rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All documents are private — only accessible via Firebase Admin SDK (server-side).
    // Android clients never access Firestore directly; they go through Cloud Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
