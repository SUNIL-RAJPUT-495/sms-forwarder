<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         PERMISSIONS — Samsung (Destination) App

         NOTE: This app does NOT request RECEIVE_SMS.
         It receives forwarded messages via FCM, never via SMS broadcast.
         ============================================================ -->

    <!--
        INTERNET: Required for FCM registration, API calls to the backend,
        and sending delivery acknowledgements.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        POST_NOTIFICATIONS: Required on Android 13+ (API 33+) to display
        forwarded SMS notifications. The user must grant this at runtime.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        ACCESS_NETWORK_STATE: Check connectivity before API calls.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        RECEIVE_BOOT_COMPLETED: Re-register FCM token and schedule
        acknowledgement retry workers after reboot.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        CAMERA: Optional — used for scanning the QR code during pairing.
        Samsung displays the QR; this is not needed unless user wants to scan
        a QR from another device in future use cases.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        USE_BIOMETRIC: Optional — if the user enables biometric auth
        gate on the Keystore private key.
    -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- PERMISSIONS NOT REQUESTED:
         RECEIVE_SMS    — Not needed; messages arrive via FCM
         READ_SMS       — Not needed
         SEND_SMS       — Not needed
         CONTACTS       — Not needed
         LOCATION       — Not needed
         MICROPHONE     — Not needed
         ACCESSIBILITY  — Not needed
    -->

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmsForwarderSamsung"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SmsForwarderSamsung">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging Service
             Receives encrypted FCM data messages from the backend.
             Decrypts and displays notification.
        -->
        <service
            android:name=".service.SamsungFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.SamsungBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Disable automatic WorkManager init for Hilt -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
